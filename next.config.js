

module.exports = {
    images: {
        // allow images from the sanity domain
        domains: ['cdn.sanity.io']
    }
    // experimental: {
    //   // allow use of the experimental next.js image component
    //   images: {
    //     allowFutureImage: true
    //   }
    // }
}